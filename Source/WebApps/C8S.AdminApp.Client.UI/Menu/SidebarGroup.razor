@inherits BaseCoordinatedComponent<SidebarGroupCoordinator>

<div id="@ComponentId" class="c8s-sidebar-group @(Service.IsSelected ? "selected" : null) @CssClass">
    <RadzenStack Orientation="Orientation.Horizontal"
                 AlignItems="AlignItems.Center"
                 JustifyContent="JustifyContent.Left"
                 onclick="@(async () => await Service.HandleClicked())"
                 class="w-100">
                 
        <IconDisplay IconString="@Service.Group.IconString"
                     CssClass="c8s-sidebar-group-icon" />

        <RadzenText TagName="TagName.Div"
                    TextStyle="TextStyle.Body1"
                    TextAlign="TextAlign.Left"
                    Text="@Service.Group.Display"
                    class="c8s-sidebar-group-display" />

    </RadzenStack>
</div>